package com.ws.ldy.constant;/** * 常量类 * * @author ws * @mail 1720696548@qq.com * @date 2020/4/27 0027 23:12 * @return */public interface BaseConstant {    /**     * 接口规则, 所有接口需添加规则, 权限扫描时会自动初始化处理类型权限到数表中, 当然也支持后期在后台变更默认配置     * uri = /api/admin    接口默认需要登录+授权     * uri = /api/client   接口默认需要登录     * uri = /api/open     接口默认可直接访问     * uri = /api/oauth2   接口默认默认需通过 appId，appSecret生成的 accessToken 来进行oauth2 令牌验证可访问     */    interface Uri {        // 请求默认开头        String api = "/api";        // 建议管理端默认        String apiAdmin = api + "/admin";        // 建议用户端默认        String apiClient = api + "/client";        // 建议通过接口默认        String apiOpen = api + "/open";        // 建议对外开放接口默认        String apiOauth2 = api + "/oauth2";    }}