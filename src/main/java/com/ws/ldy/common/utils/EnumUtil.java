package com.ws.ldy.common.utils;import com.baomidou.mybatisplus.core.enums.IEnum;public class EnumUtil {    //通过code来获得枚举,T代表枚举,书写的公共类  <T extends IEnum>这表达类，表示不确定的东西，像Interface/class    public static <T extends IEnum> T getByCode(Integer code, Class<T> enumClass) {        for (T each : enumClass.getEnumConstants()) {            if (code.equals(each.getValue())) {                return each;            }        }        return null;    }    /**     * TODO     *     * @author ws     * @mail 1720696548@qq.com     * @date 2020/5/3 0003 1:22     * @return     */    private static int req = 0;/*    public <T extends IEnum> Object getResp(Enum enums, Class<T> enumClass,) {        if ((req == 1000 || enums.ordinal() == enumClass.getEnumConstants().length - 1) && req != 1) {            req = 1000;            return enums.name();        } else {            req = 1;            if (enums.ordinal() == enumClass.getEnumConstants().length - 1) {                req = 0;            }            return JSON.parseObject("{\"value\":" + enums.value + ",\"desc\":\"" + this.desc + "\"}");        }    }*/}