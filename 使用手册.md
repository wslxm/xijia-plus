### 获取登录用户信息

- 获取token
request.getHeader(BaseConstant.Sys.TOKEN))

- 获取用户相关信息
JwtUtil.getXXX （token）
如:获取当前登录用户的Id --> JwtUtil.getUserId(request.getHeader(BaseConstant.Sys.TOKEN));
|
优化为： JwtUtil.getUser(token)


